
//function that connects to button in exampleHTML.html file
function someJavascriptFunction(){
    //variable that stores the input value gotten from the exampleHTML.html file. 
    let textFromInput = document.getElementById('userInput').value;
    
    //shows the value stored in the variable to the console. 
    console.log(textFromInput);
}